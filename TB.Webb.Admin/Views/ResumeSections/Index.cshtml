@model IEnumerable<TB.Webb.Admin.Models.ResumeSection>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContentHeader)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Resume.ResumeHeader)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.OrderByDescending(o => o.Sorter))
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContentHeader)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Resume.ResumeHeader)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
